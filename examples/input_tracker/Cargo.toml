[package]
name = "example-input-tracker"
version = "0.2.0"
edition = "2021"
license = "Apache-2.0 OR MIT"
publish = false

[dependencies]
ui-events = { workspace = true }

[features]
default = ["std"]
std = []
libm = ["ui-events/libm"]
